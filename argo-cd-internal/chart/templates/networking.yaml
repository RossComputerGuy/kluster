apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: networking
  namespace: argo-cd
spec:
  destination:
    namespace: argo-cd
    server: https://kubernetes.default.svc
  project: argo-cd-internal
  syncPolicy:
    automated: {}
    syncOptions:
      - CreateNamespace=true
  source:
    path: networking/chart
    repoURL: {{ .Values.repoURL }}
    targetRevision: {{ .Values.targetRevision }}
    helm:
      values: |
        repoURL: {{ .Values.repoURL }}
        targetRevision: {{ .Values.targetRevision }}

        {{- if (.Values.networking) -}}
        {{- toYaml .Values.networking | nindent 8 }}
        {{- end -}}
